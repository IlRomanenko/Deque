#include <iostream>
#include <fstream>
#include <memory>

using namespace std;

typedef unsigned int uint;

#define dbg(x) cerr << #x << ' ' << (x) << ' ' << __LINE__ << endl;
#define fori(i, n) for (int i = 0; i < (int)(n); i++)
